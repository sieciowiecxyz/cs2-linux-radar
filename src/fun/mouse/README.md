# fun/mouse

Host-side relay myszy przez `uinput`.

W tej wersji:

- `F9` włącza / wyłącza relay,
- `trigger` steruje synthetic LMB,
- `setup` zapisuje tylko `trigger`,
- `learn-binding` zapisuje pojedynczy `BTN_*` do configu.
